name: santexnika_backend
description: Santexnika admin va mobil ilova uchun yagona backend (Dart Shelf + JSON store).
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  path: ^1.9.0
